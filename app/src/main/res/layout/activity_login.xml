<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/color_f6f7f9">

    <ImageView
            android:id="@+id/image_logo"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginTop="106dp"
            android:contentDescription="@string/login_brand_name"
            android:src="@mipmap/ic_power_quick_action"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    <TextView
            android:id="@+id/text_brand"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/login_brand_name"
            android:textColor="@color/color_0752ae"
            android:textSize="27sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/image_logo" />

    <LinearLayout
            android:id="@+id/login_card"
            android:layout_width="0dp"
            android:layout_height="310dp"
            android:layout_marginStart="24dp"
            android:layout_marginTop="50dp"
            android:layout_marginEnd="24dp"
            android:background="@drawable/bg_login_card"
            android:elevation="2dp"
            android:gravity="center_horizontal"
            android:orientation="vertical"
            android:paddingStart="32dp"
            android:paddingTop="32dp"
            android:paddingEnd="32dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/text_brand">

        <ImageView
                android:id="@+id/image_avatar"
                android:layout_width="96dp"
                android:layout_height="96dp"
                android:clickable="true"
                android:contentDescription="@string/login_avatar_description"
                android:focusable="true"
                android:scaleType="centerCrop"
                android:src="@mipmap/ic_add_photo_outline" />

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:text="@string/login_add_avatar"
                android:textColor="@color/color_56596b"
                android:textSize="14sp" />

        <EditText
                android:id="@+id/edit_name"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="54dp"
                android:background="@drawable/bg_login_input"
                android:drawableStart="@mipmap/ic_user_profile"
                android:drawablePadding="14dp"
                android:drawableTint="@color/color_bcbbd9"
                android:hint="@string/login_name_hint"
                android:inputType="textPersonName"
                android:maxLength="30"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:singleLine="true"
                android:textColor="@color/color_17181c"
                android:textColorHint="@color/color_c9cae0"
                android:textSize="15sp" />
    </LinearLayout>

    <TextView
            android:id="@+id/button_guest_login"
            android:layout_width="252dp"
            android:layout_height="56dp"
            android:layout_marginTop="56dp"
            android:background="@drawable/bg_login_button"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:text="@string/login_guest"
            android:textColor="@color/color_ffffff"
            android:textSize="20sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/login_card" />
</androidx.constraintlayout.widget.ConstraintLayout>
